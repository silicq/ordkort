# Copy to .dev.vars (it is not committed to git) and paste your Groq key.
GROQ_API_KEY=gsk_...
# Optional: a separate salt for hashing IP addresses in the limit counters
# IP_SALT=any-long-random-string
